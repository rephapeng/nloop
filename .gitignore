__pycache__/
*.pyc
.venv/
*.db
*.db-wal
*.db-shm
# workspaces/ now holds tenant config (workspaces/<name>/config.yaml) — COMMITTED.
# Throwaway workdirs moved to .scratch/ (paths.scratch).
.scratch/
workspaces/e2e-demo/
.env
.sessions/
incoming/
*.lock
journal.jsonl
.nloop/
