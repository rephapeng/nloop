__pycache__/
*.pyc
.venv/
*.db
*.db-wal
*.db-shm
# workspaces/ sekarang = config tenant (workspaces/<nama>/config.yaml) — DI-COMMIT.
# Workdir sekali pakai pindah ke .scratch/ (paths.scratch).
.scratch/
workspaces/e2e-demo/
.env
.sessions/
incoming/
*.lock
journal.jsonl
.nloop/
