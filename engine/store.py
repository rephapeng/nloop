"""SQLite (WAL): runs, iterations, events.  TODO(Fase 2)

Queue = kolom runs.status='queued' — nggak ada tabel jobs terpisah (tahan restart).
Schema lengkap ada di PLAN.md.
"""
from __future__ import annotations
